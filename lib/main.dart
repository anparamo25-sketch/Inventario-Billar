import 'dart:async';
import 'dart:convert';
import 'package:flutter/material.dart';
import 'package:intl/intl.dart';
import 'package:pdf/widgets.dart' as pw;
import 'package:printing/printing.dart';
import 'package:shared_preferences/shared_preferences.dart';

void main()=>runApp(const App());
class Product{final String name;final int price,packSize;int warehouse,freezer;Product(this.name,this.price,this.packSize,{this.warehouse=0,this.freezer=0});Map<String,dynamic> j()=>{'name':name,'warehouse':warehouse,'freezer':freezer};}
class Loan{final String person,reason;final int amount;final DateTime date;Loan(this.person,this.amount,this.reason,this.date);Map<String,dynamic> j()=>{'person':person,'amount':amount,'reason':reason,'date':date.toIso8601String()};}
class Consumption{final String person,product;final int quantity,value;final DateTime date;Consumption(this.person,this.product,this.quantity,this.value,this.date);Map<String,dynamic> j()=>{'person':person,'product':product,'quantity':quantity,'value':value,'date':date.toIso8601String()};}
class Movement{final String product,type;final int units;final DateTime date;Movement(this.product,this.units,this.date,{this.type='Bodega → Freezer'});Map<String,dynamic> j()=>{'product':product,'units':units,'type':type,'date':date.toIso8601String()};}
class Expense{final String concept,observation;final int amount;final DateTime date;Expense(this.concept,this.amount,this.observation,this.date);Map<String,dynamic> j()=>{'concept':concept,'amount':amount,'observation':observation,'date':date.toIso8601String()};}
class Order{final DateTime date;final String supplier,observation;final int paid;final Map<String,int> warehouseCases,cigaretteUnits;Order(this.date,this.supplier,this.paid,this.warehouseCases,this.cigaretteUnits,this.observation);Map<String,dynamic> j()=>{'date':date.toIso8601String(),'supplier':supplier,'paid':paid,'warehouseCases':warehouseCases,'cigaretteUnits':cigaretteUnits,'observation':observation};static Map<String,int> mp(dynamic v){final m=Map<String,dynamic>.from(v??{});return m.map((k,v)=>MapEntry(k,(v as num).toInt()));}factory Order.from(Map<String,dynamic>x)=>Order(DateTime.tryParse(x['date']??'')??DateTime.now(),x['supplier']??'',(x['paid'] as num? ?? 0).toInt(),mp(x['warehouseCases']),mp(x['cigaretteUnits']),x['observation']??'');}
class Cigarette{final String name;int price,stock;Cigarette(this.name,{this.price=0,this.stock=0});Map<String,dynamic> j()=>{'name':name,'price':price,'stock':stock};}
class Day{final DateTime date;final Map<String,int> opening,finalStock,sold,refill,cigaretteOpening,cigaretteFinal,cigaretteSold,cigaretteEntries;final List<Loan> loans;final List<Consumption> consumptions;final List<Movement> movements;final List<Expense> expenses;final List<Order> orders;final int sales,expected,actual,difference;Day({required this.date,required this.opening,required this.finalStock,required this.sold,required this.refill,required this.loans,required this.consumptions,required this.movements,required this.expenses,required this.orders,required this.cigaretteOpening,required this.cigaretteFinal,required this.cigaretteSold,required this.cigaretteEntries,required this.sales,required this.expected,required this.actual,required this.difference});Map<String,dynamic> j()=>{'date':date.toIso8601String(),'opening':opening,'final':finalStock,'sold':sold,'refill':refill,'loans':loans.map((x)=>x.j()).toList(),'consumptions':consumptions.map((x)=>x.j()).toList(),'movements':movements.map((x)=>x.j()).toList(),'expenses':expenses.map((x)=>x.j()).toList(),'orders':orders.map((x)=>x.j()).toList(),'cigaretteOpening':cigaretteOpening,'cigaretteFinal':cigaretteFinal,'cigaretteSold':cigaretteSold,'cigaretteEntries':cigaretteEntries,'sales':sales,'expected':expected,'actual':actual,'difference':difference};static Map<String,int> mp(dynamic v){final m=Map<String,dynamic>.from(v??{});return m.map((k,v)=>MapEntry(k,(v as num).toInt()));}factory Day.from(Map<String,dynamic> x)=>Day(date:DateTime.tryParse(x['date']??'')??DateTime.now(),opening:mp(x['opening']),finalStock:mp(x['final']),sold:mp(x['sold']),refill:mp(x['refill']),loans:(x['loans'] as List? ?? []).map((v)=>Loan((v['person']??'') as String,(v['amount'] as num? ?? 0).toInt(),(v['reason']??'') as String,DateTime.tryParse(v['date']??'')??DateTime.now())).toList(),consumptions:(x['consumptions'] as List? ?? []).map((v)=>Consumption((v['person']??'') as String,(v['product']??'') as String,(v['quantity'] as num? ?? 0).toInt(),(v['value'] as num? ?? 0).toInt(),DateTime.tryParse(v['date']??'')??DateTime.now())).toList(),expenses:(x['expenses'] as List? ?? []).map((v)=>Expense((v['concept']??'') as String,(v['amount'] as num? ?? 0).toInt(),(v['observation']??'') as String,DateTime.tryParse(v['date']??'')??DateTime.now())).toList(),orders:(x['orders'] as List? ?? []).map((v)=>Order.from(Map<String,dynamic>.from(v))).toList(),cigaretteOpening:mp(x['cigaretteOpening']),cigaretteFinal:mp(x['cigaretteFinal']),cigaretteSold:mp(x['cigaretteSold']),cigaretteEntries:mp(x['cigaretteEntries']),movements:(x['movements'] as List? ?? []).map((v)=>Movement((v['product']??'') as String,(v['units'] as num? ?? 0).toInt(),DateTime.tryParse(v['date']??'')??DateTime.now(),type:(v['type']??'Bodega → Freezer') as String)).toList(),sales:(x['sales'] as num? ?? 0).toInt(),expected:(x['expected'] as num? ?? 0).toInt(),actual:(x['actual'] as num? ?? 0).toInt(),difference:(x['difference'] as num? ?? 0).toInt());}
final ps=<Product>[Product('Toña pequeña',46,24),Product('Litro Toña',95,12),Product('Frost litro',80,12),Product('Frost pequeña',40,24),Product('Gaseosa',25,24)];
final cigarettes=<Cigarette>[Cigarette('Cigarros')];final expenses=<Expense>[];final orders=<Order>[];final cigaretteOpening=<String,int>{};final cigaretteEntries=<String,int>{};
class Store{static Future<SharedPreferences> get p=>SharedPreferences.getInstance();static Future<void> save()async{final x=await p;await x.setString('products',jsonEncode(ps.map((p)=>p.j()).toList()));await x.setString('history',jsonEncode(H.map((d)=>d.j()).toList()));await x.setString('opening',jsonEncode(opening));await x.setString('date',dayDate.toIso8601String());await x.setBool('jornadaAbierta',jornadaAbiertaGlobal);await x.setString('cigarettes',jsonEncode(cigarettes.map((c)=>c.j()).toList()));await x.setString('expenses',jsonEncode(expenses.map((e)=>e.j()).toList()));await x.setString('orders',jsonEncode(orders.map((o)=>o.j()).toList()));await x.setString('cigaretteOpening',jsonEncode(cigaretteOpening));await x.setString('cigaretteEntries',jsonEncode(cigaretteEntries));}static Future<Map<String,dynamic>> load()async{final x=await p;dynamic d(String k,dynamic f){try{return jsonDecode(x.getString(k)??jsonEncode(f));}catch(_){return f;}}return{'products':d('products',[]),'history':d('history',[]),'opening':d('opening',{}),'date':x.getString('date'),'jornadaAbierta':x.getBool('jornadaAbierta')??false,'cigarettes':d('cigarettes',[]),'expenses':d('expenses',[]),'orders':d('orders',[]),'cigaretteOpening':d('cigaretteOpening',{}),'cigaretteEntries':d('cigaretteEntries',{})};}}
final H=<Day>[];final opening=<String,int>{};DateTime dayDate=DateTime.now();bool jornadaAbiertaGlobal=false;
class App extends StatelessWidget{const App({super.key});@override Widget build(BuildContext c)=>MaterialApp(debugShowCheckedModeBanner:false,title:'Inventario Billar',theme:ThemeData(useMaterial3:true,colorSchemeSeed:Colors.indigo),home:const Home());}
class Home extends StatefulWidget{const Home({super.key});@override State<Home> createState()=>_HomeState();}
class _HomeState extends State<Home> with WidgetsBindingObserver{int tab=0;bool loading=true,closed=false,jornadaAbierta=false;Timer? auto;final loans=<Loan>[],cons=<Consumption>[],mov=<Movement>[];int lastSales=0,lastExpected=0;
 @override void initState(){super.initState();WidgetsBinding.instance.addObserver(this);load();auto=Timer.periodic(const Duration(seconds:15),(_)=>Store.save());}
 bool same(DateTime a,DateTime b)=>a.year==b.year&&a.month==b.month&&a.day==b.day;
 Future<void> load()async{final d=await Store.load();for(final v in d['products'] as List){for(final p in ps){if(p.name==v['name']){p.warehouse=(v['warehouse'] as num? ?? 0).toInt();p.freezer=(v['freezer'] as num? ?? 0).toInt();}}}H.addAll((d['history'] as List).map((v)=>Day.from(Map<String,dynamic>.from(v))));opening.addAll(Day.mp(d['opening']));for(final v in d['cigarettes'] as List){final m=Map<String,dynamic>.from(v);final c=cigarettes.firstWhere((x)=>x.name==m['name'],orElse:()=>Cigarette(m['name']));c.price=(m['price'] as num? ?? 0).toInt();c.stock=(m['stock'] as num? ?? 0).toInt();if(!cigarettes.contains(c))cigarettes.add(c);}expenses.addAll((d['expenses'] as List).map((v){final m=Map<String,dynamic>.from(v);return Expense(m['concept']??'',(m['amount'] as num? ?? 0).toInt(),m['observation']??'',DateTime.tryParse(m['date']??'')??DateTime.now());}));orders.addAll((d['orders'] as List).map((v)=>Order.from(Map<String,dynamic>.from(v))));cigaretteOpening.addAll(Day.mp(d['cigaretteOpening']));cigaretteEntries.addAll(Day.mp(d['cigaretteEntries']));dayDate=DateTime.tryParse(d['date']??'')??DateTime.now();if(opening.isEmpty){for(final p in ps)opening[p.name]=p.freezer;}if(cigaretteOpening.isEmpty){for(final c in cigarettes)cigaretteOpening[c.name]=c.stock;}if(!same(dayDate,DateTime.now())){dayDate=DateTime.now();opening..clear()..addEntries(ps.map((p)=>MapEntry(p.name,p.freezer)));jornadaAbierta=false;closed=false;await Store.save();}if(H.isNotEmpty&&same(H.last.date,dayDate)){closed=true;jornadaAbierta=false;}else{jornadaAbierta=(d['jornadaAbierta'] as bool? ?? false);closed=false;}jornadaAbiertaGlobal=jornadaAbierta;if(H.isNotEmpty){lastSales=H.last.sales;lastExpected=H.last.expected;}setState(()=>loading=false);}
 Future<void> change(VoidCallback f)async{setState(f);jornadaAbiertaGlobal=jornadaAbierta;await Store.save();}
 @override void didChangeAppLifecycleState(AppLifecycleState s){if(s==AppLifecycleState.inactive||s==AppLifecycleState.paused||s==AppLifecycleState.detached)Store.save();}
 @override void dispose(){auto?.cancel();WidgetsBinding.instance.removeObserver(this);jornadaAbiertaGlobal=jornadaAbierta;Store.save();super.dispose();}
 @override Widget build(BuildContext c){if(loading)return const Scaffold(body:Center(child:CircularProgressIndicator()));final pages=[home(),warehouse(),freezer(),History(history:H)];return Scaffold(appBar:AppBar(title:const Text('Inventario Billar')),body:pages[tab],bottomNavigationBar:NavigationBar(selectedIndex:tab,onDestinationSelected:(i)=>setState(()=>tab=i),destinations:const[NavigationDestination(icon:Icon(Icons.dashboard),label:'Inicio'),NavigationDestination(icon:Icon(Icons.warehouse),label:'Bodega'),NavigationDestination(icon:Icon(Icons.ac_unit),label:'Freezer'),NavigationDestination(icon:Icon(Icons.history),label:'Historial')]),floatingActionButton:jornadaAbierta&&!closed?FloatingActionButton.extended(onPressed:closeDay,icon:const Icon(Icons.lock_clock),label:const Text('Cerrar jornada')):null);}
 Widget home(){final b=ps.fold(0,(s,p)=>s+p.warehouse),f=ps.fold(0,(s,p)=>s+p.freezer);return ListView(padding:const EdgeInsets.all(16),children:[Text('Resumen de hoy',style:Theme.of(context).textTheme.headlineSmall),Text(DateFormat('dd/MM/yyyy').format(dayDate)),if(!jornadaAbierta)Card(child:Padding(padding:const EdgeInsets.all(16),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Jornada pendiente de apertura',style:TextStyle(fontSize:18,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Antes de comenzar, verifica las unidades de cada producto que deben estar en el freezer.'),const SizedBox(height:12),SizedBox(width:double.infinity,child:FilledButton.icon(onPressed:openDay,icon:const Icon(Icons.play_arrow),label:const Text('Abrir jornada')))]))),if(closed)Card(child:Padding(padding:const EdgeInsets.all(16),child:Row(children:[const Icon(Icons.check_circle),const SizedBox(width:10),Expanded(child:Text('Jornada cerrada y registrada.',style:const TextStyle(fontWeight:FontWeight.bold)))]))),Row(children:[Expanded(child:card('Bodega','$b unidades')),Expanded(child:card('Freezer','$f unidades'))]),if(jornadaAbierta)ListTile(title:const Text('Jornada abierta'),subtitle:const Text('El control de ventas y movimientos del día está activo.')),ListTile(title:const Text('Ventas último cierre'),trailing:Text('C\$$lastSales')),FilledButton.icon(onPressed:jornadaAbierta&&!closed?addLoan:null,icon:const Icon(Icons.payments),label:const Text('Registrar préstamo / retiro')),OutlinedButton.icon(onPressed:jornadaAbierta&&!closed?addConsumption:null,icon:const Icon(Icons.restaurant),label:const Text('Registrar consumo del propietario')),OutlinedButton.icon(onPressed:jornadaAbierta&&!closed?addExpense:null,icon:const Icon(Icons.money_off),label:const Text('Registrar gasto')),FilledButton.icon(onPressed:jornadaAbierta&&!closed?addOrder:null,icon:const Icon(Icons.inventory_2),label:const Text('Registrar pedido')),OutlinedButton.icon(onPressed:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>CigarettePage(cigarettes))),icon:const Icon(Icons.smoking_rooms),label:const Text('Control de cigarros')),const Card(child:ListTile(leading:Icon(Icons.save),title:Text('Autoguardado activo'),subtitle:Text('Guarda cada 15 segundos y al pasar la aplicación a segundo plano.')))]);}
 Widget card(String a,String b)=>Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(a),Text(b,style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold))])));
 Widget warehouse()=>ListView(padding:const EdgeInsets.all(16),children:[const Text('Bodega',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const Text('Las entradas se agregan por cajillas y el sistema las convierte a unidades.'),...ps.map((p)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(p.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('${p.warehouse~/p.packSize} cajillas + ${p.warehouse%p.packSize} unidades'),Text('1 cajilla = ${p.packSize} unidades'),Row(children:[Expanded(child:OutlinedButton(onPressed:closed?null:addOrder,child:const Text('Agregar por cajillas'))),Expanded(child:FilledButton(onPressed:closed?null:()=>move(p),child:const Text('Pasar a freezer')))])]))))]);
 Widget freezer()=>ListView(padding:const EdgeInsets.all(16),children:[const Text('Freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...ps.map((p)=>Card(child:ListTile(title:Text(p.name),subtitle:Text('Actual: ${p.freezer} • Esperado al abrir: ${opening[p.name]??0}'),trailing:Text('C\$${p.price}'))))]);
 Future<void> openDay()async{final ok=await Navigator.push<bool>(context,MaterialPageRoute(builder:(_)=>OpenDayPage(ps,opening)));if(ok==true&&mounted){await change((){jornadaAbierta=true;closed=false;});}}
 Future<void> edit(Product p)async{final c=TextEditingController(text:'${p.warehouse}');final v=await showDialog<int>(context:context,builder:(_)=>AlertDialog(title:Text(p.name),content:TextField(controller:c,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades')),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,int.tryParse(c.text)??0),child:const Text('Guardar'))]));c.dispose();if(v!=null&&v>=0)await change(()=>p.warehouse=v);}
 Future<void> move(Product p)async{final c=TextEditingController();final v=await showDialog<int>(context:context,builder:(_)=>AlertDialog(title:Text('Pasar ${p.name}'),content:TextField(controller:c,keyboardType:TextInputType.number,decoration:InputDecoration(labelText:'Unidades',helperText:'Disponible: ${p.warehouse}')),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,int.tryParse(c.text)??0),child:const Text('Trasladar'))]));c.dispose();if(v==null||v<=0||v>p.warehouse)return;if(mounted)await change((){p.warehouse-=v;p.freezer+=v;mov.add(Movement(p.name,v,DateTime.now()));});}
 Future<void> addLoan()async{final a=TextEditingController(),m=TextEditingController(),r=TextEditingController();final ok=await showDialog<bool>(context:context,builder:(_)=>AlertDialog(title:const Text('Préstamo / retiro'),content:Column(mainAxisSize:MainAxisSize.min,children:[TextField(controller:a,decoration:const InputDecoration(labelText:'Persona')),TextField(controller:m,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Monto C\$')),TextField(controller:r,decoration:const InputDecoration(labelText:'Motivo'))]),actions:[TextButton(onPressed:()=>Navigator.pop(context,false),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,true),child:const Text('Guardar'))]));if(ok==true&&(int.tryParse(m.text)??0)>0&&a.text.trim().isNotEmpty)await change(()=>loans.add(Loan(a.text.trim(),int.parse(m.text),r.text.trim(),DateTime.now())));a.dispose();m.dispose();r.dispose();}
 Future<void> addConsumption()async{String? sel;final who=TextEditingController(),q=TextEditingController(text:'1');final ok=await showDialog<bool>(context:context,builder:(_)=>StatefulBuilder(builder:(c,setD)=>AlertDialog(title:const Text('Consumo del propietario'),content:Column(mainAxisSize:MainAxisSize.min,children:[TextField(controller:who,decoration:const InputDecoration(labelText:'Quién')),DropdownButtonFormField<String>(decoration:const InputDecoration(labelText:'Producto'),items:ps.map((p)=>DropdownMenuItem(value:p.name,child:Text(p.name))).toList(),onChanged:(v)=>setD(()=>sel=v)),TextField(controller:q,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Cantidad'))]),actions:[TextButton(onPressed:()=>Navigator.pop(c,false),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(c,true),child:const Text('Guardar'))])));if(ok==true&&sel!=null){final p=ps.firstWhere((x)=>x.name==sel);final n=int.tryParse(q.text)??0;if(n>0&&n<=p.freezer&&who.text.trim().isNotEmpty)await change((){p.freezer-=n;cons.add(Consumption(who.text.trim(),p.name,n,n*p.price,DateTime.now()));});}who.dispose();q.dispose();}
 Future<void> addExpense()async{final a=TextEditingController(),m=TextEditingController(),o=TextEditingController();final ok=await showDialog<bool>(context:context,builder:(_)=>AlertDialog(title:const Text('Gasto del día'),content:Column(mainAxisSize:MainAxisSize.min,children:[TextField(controller:a,decoration:const InputDecoration(labelText:'Concepto')),TextField(controller:m,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Monto C\final counts=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>ClosePage(ps,cs,opening)));for(final c in cs.values)c.dispose();if(counts==null)return;final sold=<String,int>{};int sales=0;final cigOpen={for(final c in cigarettes)c.name:(cigaretteOpening[c.name]??c.stock)};final cigFinal={for(final c in cigarettes)c.name:c.stock};final cigSold={for(final c in cigarettes)c.name:(cigOpen[c.name]??0)+(cigaretteEntries[c.name]??0)-c.stock};for(final p in ps){final n=counts[p.name]!;final s=p.freezer-n;if(s<0)return;p.freezer=n;if(s>0){sold[p.name]=s;sales+=s*p.price;}}for(final c in cigarettes){if((cigSold[c.name]??0)<0)return;sales+=(cigSold[c.name]??0)*c.price;}final lt=loans.fold(0,(s,x)=>s+x.amount),ct=cons.fold(0,(s,x)=>s+x.value),gt=expenses.fold(0,(s,x)=>s+x.amount),pt=orders.fold(0,(s,x)=>s+x.paid),expected=sales-lt-ct-gt-pt;final actual=await Navigator.push<int>(context,MaterialPageRoute(builder:(_)=>CashPage(sales,lt,ct,expected)));if(actual==null)return;final diff=actual-expected;await showDialog(context:context,barrierDismissible:false,builder:(_)=>AlertDialog(title:const Text('Arqueo final'),content:Text(diff==0?'Exacto: C\$0':diff<0?'Faltante: C\$${diff.abs()}':'Sobrante: C\$${diff.abs()}'),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('Continuar'))]));final refill=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>RefillPage(ps)));if(refill==null)return;final pre={for(final p in ps)p.name:p.freezer};await change((){for(final p in ps){final n=refill[p.name]??0;p.warehouse-=n;p.freezer+=n;if(n>0)mov.add(Movement(p.name,n,DateTime.now(),type:'Bodega → Freezer (Relleno)'));}H.add(Day(date:dayDate,opening:Map.from(opening),finalStock:pre,sold:sold,refill:Map.from(refill),loans:List.from(loans),consumptions:List.from(cons),movements:List.from(mov),expenses:List.from(expenses),orders:List.from(orders),cigaretteOpening:Map.from(cigOpen),cigaretteFinal:Map.from(cigFinal),cigaretteSold:Map.from(cigSold),cigaretteEntries:Map.from(cigaretteEntries),sales:sales,expected:expected,actual:actual,difference:diff));lastSales=sales;lastExpected=expected;opening..clear()..addEntries(ps.map((p)=>MapEntry(p.name,p.freezer)));loans.clear();cons.clear();mov.clear();expenses.clear();orders.clear();cigaretteEntries.clear();for(final c in cigarettes)cigaretteOpening[c.name]=c.stock;closed=true;jornadaAbierta=false;});}
}
class OrderPage extends StatefulWidget{final List<Product> products;final List<Cigarette> cigarettes;const OrderPage(this.products,this.cigarettes,{super.key});@override State<OrderPage> createState()=>_OrderState();}
class _OrderState extends State<OrderPage>{late final Map<String,TextEditingController> cases;late final Map<String,TextEditingController> cigs;final supplier=TextEditingController(),paid=TextEditingController(),obs=TextEditingController();DateTime date=DateTime.now();@override void initState(){super.initState();cases={for(final p in widget.products)p.name:TextEditingController(text:'0')};cigs={for(final c in widget.cigarettes)c.name:TextEditingController(text:'0')};}@override void dispose(){for(final c in cases.values)c.dispose();for(final c in cigs.values)c.dispose();supplier.dispose();paid.dispose();obs.dispose();super.dispose();}Future<void> pickDate()async{final d=await showDatePicker(context:context,firstDate:DateTime(2020),lastDate:DateTime(2100),initialDate:date);if(d!=null)setState(()=>date=DateTime(d.year,d.month,d.day,date.hour,date.minute));}@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Registrar pedido')),body:ListView(padding:const EdgeInsets.all(16),children:[ListTile(title:const Text('Fecha exacta de llegada'),subtitle:Text(DateFormat('dd/MM/yyyy HH:mm').format(date)),trailing:IconButton(onPressed:pickDate,icon:const Icon(Icons.calendar_month))),TextField(controller:supplier,decoration:const InputDecoration(labelText:'Proveedor (opcional)')),const SizedBox(height:12),const Text('Productos de Bodega',style:TextStyle(fontSize:18,fontWeight:FontWeight.bold)),...widget.products.map((p)=>Card(child:TextField(controller:cases[p.name],keyboardType:TextInputType.number,decoration:InputDecoration(labelText:'Cajillas de ${p.name}',helperText:'1 cajilla = ${p.packSize} unidades')))),const Text('Cigarros',style:TextStyle(fontSize:18,fontWeight:FontWeight.bold)),...widget.cigarettes.map((c)=>Card(child:TextField(controller:cigs[c.name],keyboardType:TextInputType.number,decoration:InputDecoration(labelText:'Unidades de ${c.name}')))),TextField(controller:paid,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Total pagado C\ extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos, consumos, gastos y pedidos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
)),TextField(controller:o,decoration:const InputDecoration(labelText:'Observación'))]),actions:[TextButton(onPressed:()=>Navigator.pop(context,false),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,true),child:const Text('Guardar'))]));if(ok==true&&(int.tryParse(m.text)??0)>0&&a.text.trim().isNotEmpty)await change(()=>expenses.add(Expense(a.text.trim(),int.parse(m.text),o.text.trim(),DateTime.now())));a.dispose();m.dispose();o.dispose();}
 Future<void> addOrder()async{final r=await Navigator.push<Order>(context,MaterialPageRoute(builder:(_)=>OrderPage(ps,cigarettes)));if(r==null)return;await change((){for(final p in ps){final n=r.warehouseCases[p.name]??0;if(n>0)p.warehouse+=n*p.packSize;}for(final c in cigarettes){final n=r.cigaretteUnits[c.name]??0;if(n>0){c.stock+=n;cigaretteEntries[c.name]=(cigaretteEntries[c.name]??0)+n;}}orders.add(r);});}
 Future<void> closeDay()async{final cs={for(final p in ps)p.name:TextEditingController(text:'${p.freezer}')};final counts=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>ClosePage(ps,cs,opening)));for(final c in cs.values)c.dispose();if(counts==null)return;final sold=<String,int>{};int sales=0;for(final p in ps){final n=counts[p.name]!;final s=p.freezer-n;if(s<0)return;p.freezer=n;if(s>0){sold[p.name]=s;sales+=s*p.price;}}final lt=loans.fold(0,(s,x)=>s+x.amount),ct=cons.fold(0,(s,x)=>s+x.value),expected=sales-lt-ct;final actual=await Navigator.push<int>(context,MaterialPageRoute(builder:(_)=>CashPage(sales,lt,ct,expected)));if(actual==null)return;final diff=actual-expected;await showDialog(context:context,barrierDismissible:false,builder:(_)=>AlertDialog(title:const Text('Arqueo final'),content:Text(diff==0?'Exacto: C\$0':diff<0?'Faltante: C\$${diff.abs()}':'Sobrante: C\$${diff.abs()}'),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('Continuar'))]));final refill=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>RefillPage(ps)));if(refill==null)return;final pre={for(final p in ps)p.name:p.freezer};await change((){for(final p in ps){final n=refill[p.name]??0;p.warehouse-=n;p.freezer+=n;if(n>0)mov.add(Movement(p.name,n,DateTime.now(),type:'Bodega → Freezer (Relleno)'));}H.add(Day(date:dayDate,opening:Map.from(opening),finalStock:pre,sold:sold,refill:Map.from(refill),loans:List.from(loans),consumptions:List.from(cons),movements:List.from(mov),sales:sales,expected:expected,actual:actual,difference:diff));lastSales=sales;lastExpected=expected;opening..clear()..addEntries(ps.map((p)=>MapEntry(p.name,p.freezer)));loans.clear();cons.clear();mov.clear();closed=true;jornadaAbierta=false;});}
}
class OpenDayPage extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos y consumos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
)),TextField(controller:obs,decoration:const InputDecoration(labelText:'Observación')),const SizedBox(height:12),FilledButton(onPressed:(){final wc={for(final p in widget.products)p.name:int.tryParse(cases[p.name]!.text)??0};final cu={for(final c in widget.cigarettes)c.name:int.tryParse(cigs[c.name]!.text)??0};Navigator.pop(x,Order(date,supplier.text.trim(),int.tryParse(paid.text)??0,wc,cu,obs.text.trim()));},child:const Text('Guardar pedido'))]));}
class CigarettePage extends StatefulWidget{final List<Cigarette> cigarettes;const CigarettePage(this.cigarettes,{super.key});@override State<CigarettePage> createState()=>_CigState();}
class _CigState extends State<CigarettePage>{@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Control de cigarros')),body:ListView(padding:const EdgeInsets.all(16),children:[...widget.cigarettes.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Existencia actual: ${z.stock}'),Text('Inicial de jornada: ${cigaretteOpening[z.name]??z.stock}'),Text('Entradas del día: ${cigaretteEntries[z.name]??0}'),TextField(keyboardType:TextInputType.number,controller:TextEditingController(text:'${z.price}'),decoration:const InputDecoration(labelText:'Precio de venta C\ extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos, consumos, gastos y pedidos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
)),TextField(controller:o,decoration:const InputDecoration(labelText:'Observación'))]),actions:[TextButton(onPressed:()=>Navigator.pop(context,false),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,true),child:const Text('Guardar'))]));if(ok==true&&(int.tryParse(m.text)??0)>0&&a.text.trim().isNotEmpty)await change(()=>expenses.add(Expense(a.text.trim(),int.parse(m.text),o.text.trim(),DateTime.now())));a.dispose();m.dispose();o.dispose();}
 Future<void> addOrder()async{final r=await Navigator.push<Order>(context,MaterialPageRoute(builder:(_)=>OrderPage(ps,cigarettes)));if(r==null)return;await change((){for(final p in ps){final n=r.warehouseCases[p.name]??0;if(n>0)p.warehouse+=n*p.packSize;}for(final c in cigarettes){final n=r.cigaretteUnits[c.name]??0;if(n>0){c.stock+=n;cigaretteEntries[c.name]=(cigaretteEntries[c.name]??0)+n;}}orders.add(r);});}
 Future<void> closeDay()async{final cs={for(final p in ps)p.name:TextEditingController(text:'${p.freezer}')};final counts=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>ClosePage(ps,cs,opening)));for(final c in cs.values)c.dispose();if(counts==null)return;final sold=<String,int>{};int sales=0;for(final p in ps){final n=counts[p.name]!;final s=p.freezer-n;if(s<0)return;p.freezer=n;if(s>0){sold[p.name]=s;sales+=s*p.price;}}final lt=loans.fold(0,(s,x)=>s+x.amount),ct=cons.fold(0,(s,x)=>s+x.value),expected=sales-lt-ct;final actual=await Navigator.push<int>(context,MaterialPageRoute(builder:(_)=>CashPage(sales,lt,ct,expected)));if(actual==null)return;final diff=actual-expected;await showDialog(context:context,barrierDismissible:false,builder:(_)=>AlertDialog(title:const Text('Arqueo final'),content:Text(diff==0?'Exacto: C\$0':diff<0?'Faltante: C\$${diff.abs()}':'Sobrante: C\$${diff.abs()}'),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('Continuar'))]));final refill=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>RefillPage(ps)));if(refill==null)return;final pre={for(final p in ps)p.name:p.freezer};await change((){for(final p in ps){final n=refill[p.name]??0;p.warehouse-=n;p.freezer+=n;if(n>0)mov.add(Movement(p.name,n,DateTime.now(),type:'Bodega → Freezer (Relleno)'));}H.add(Day(date:dayDate,opening:Map.from(opening),finalStock:pre,sold:sold,refill:Map.from(refill),loans:List.from(loans),consumptions:List.from(cons),movements:List.from(mov),sales:sales,expected:expected,actual:actual,difference:diff));lastSales=sales;lastExpected=expected;opening..clear()..addEntries(ps.map((p)=>MapEntry(p.name,p.freezer)));loans.clear();cons.clear();mov.clear();closed=true;jornadaAbierta=false;});}
}
class OpenDayPage extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos y consumos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
),onChanged:(v){z.price=int.tryParse(v)??z.price;Store.save();}),])))),const SizedBox(height:8),const Text('Las compras de cigarros se registran desde Pedidos y se suman automáticamente a la existencia.') ]));}
}class OpenDayPage extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos, consumos, gastos y pedidos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
)),TextField(controller:o,decoration:const InputDecoration(labelText:'Observación'))]),actions:[TextButton(onPressed:()=>Navigator.pop(context,false),child:const Text('Cancelar')),FilledButton(onPressed:()=>Navigator.pop(context,true),child:const Text('Guardar'))]));if(ok==true&&(int.tryParse(m.text)??0)>0&&a.text.trim().isNotEmpty)await change(()=>expenses.add(Expense(a.text.trim(),int.parse(m.text),o.text.trim(),DateTime.now())));a.dispose();m.dispose();o.dispose();}
 Future<void> addOrder()async{final r=await Navigator.push<Order>(context,MaterialPageRoute(builder:(_)=>OrderPage(ps,cigarettes)));if(r==null)return;await change((){for(final p in ps){final n=r.warehouseCases[p.name]??0;if(n>0)p.warehouse+=n*p.packSize;}for(final c in cigarettes){final n=r.cigaretteUnits[c.name]??0;if(n>0){c.stock+=n;cigaretteEntries[c.name]=(cigaretteEntries[c.name]??0)+n;}}orders.add(r);});}
 Future<void> closeDay()async{final cs={for(final p in ps)p.name:TextEditingController(text:'${p.freezer}')};final counts=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>ClosePage(ps,cs,opening)));for(final c in cs.values)c.dispose();if(counts==null)return;final sold=<String,int>{};int sales=0;for(final p in ps){final n=counts[p.name]!;final s=p.freezer-n;if(s<0)return;p.freezer=n;if(s>0){sold[p.name]=s;sales+=s*p.price;}}final lt=loans.fold(0,(s,x)=>s+x.amount),ct=cons.fold(0,(s,x)=>s+x.value),expected=sales-lt-ct;final actual=await Navigator.push<int>(context,MaterialPageRoute(builder:(_)=>CashPage(sales,lt,ct,expected)));if(actual==null)return;final diff=actual-expected;await showDialog(context:context,barrierDismissible:false,builder:(_)=>AlertDialog(title:const Text('Arqueo final'),content:Text(diff==0?'Exacto: C\$0':diff<0?'Faltante: C\$${diff.abs()}':'Sobrante: C\$${diff.abs()}'),actions:[FilledButton(onPressed:()=>Navigator.pop(context),child:const Text('Continuar'))]));final refill=await Navigator.push<Map<String,int>>(context,MaterialPageRoute(builder:(_)=>RefillPage(ps)));if(refill==null)return;final pre={for(final p in ps)p.name:p.freezer};await change((){for(final p in ps){final n=refill[p.name]??0;p.warehouse-=n;p.freezer+=n;if(n>0)mov.add(Movement(p.name,n,DateTime.now(),type:'Bodega → Freezer (Relleno)'));}H.add(Day(date:dayDate,opening:Map.from(opening),finalStock:pre,sold:sold,refill:Map.from(refill),loans:List.from(loans),consumptions:List.from(cons),movements:List.from(mov),sales:sales,expected:expected,actual:actual,difference:diff));lastSales=sales;lastExpected=expected;opening..clear()..addEntries(ps.map((p)=>MapEntry(p.name,p.freezer)));loans.clear();cons.clear();mov.clear();closed=true;jornadaAbierta=false;});}
}
class OpenDayPage extends StatefulWidget{final List<Product> products;final Map<String,int> expected;const OpenDayPage(this.products,this.expected,{super.key});@override State<OpenDayPage> createState()=>_OpenDayState();}
class _OpenDayState extends State<OpenDayPage>{final checked=<String,bool>{};@override void initState(){super.initState();for(final p in widget.products)checked[p.name]=false;}bool get allChecked=>checked.values.every((v)=>v);@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Abrir jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Verificación inicial del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:8),const Text('Confirma con un check cada producto después de comprobar físicamente las unidades indicadas.'),const SizedBox(height:12),...widget.products.map((p)=>Card(child:CheckboxListTile(value:checked[p.name]??false,onChanged:(v)=>setState(()=>checked[p.name]=v??false),title:Text(p.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('Hay ${widget.expected[p.name]??0} unidades en freezer'),secondary:CircleAvatar(child:Text('${widget.expected[p.name]??0}'))))),const SizedBox(height:12),FilledButton.icon(onPressed:allChecked?()=>Navigator.pop(x,true):null,icon:const Icon(Icons.lock_open),label:const Text('Confirmar y abrir jornada'))]));}
class ClosePage extends StatelessWidget{final List<Product> p;final Map<String,TextEditingController> c;final Map<String,int> o;const ClosePage(this.p,this.c,this.o,{super.key});@override Widget build(BuildContext x)=>Scaffold(appBar:AppBar(title:const Text('Cierre de jornada')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Inventario final del freezer',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),Text('Esperado al abrir: ${o[z.name]??0} • Disponible: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades que quedan')),ValueListenableBuilder<TextEditingValue>(valueListenable:c[z.name]!,builder:(_,v,__){final n=int.tryParse(v.text)??0;final s=(z.freezer-n).clamp(0,z.freezer);return Text('Venta calculada: $s unidades • C\$${s*z.price}',style:const TextStyle(fontWeight:FontWeight.bold));})])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??-1;if(n<0||n>z.freezer)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y continuar al arqueo'))]));}
class CashPage extends StatefulWidget{final int sales,loans,cons,expected;const CashPage(this.sales,this.loans,this.cons,this.expected,{super.key});@override State<CashPage> createState()=>_CashState();}
class _CashState extends State<CashPage>{final c=TextEditingController();@override Widget build(BuildContext x){final a=int.tryParse(c.text),d=a==null?null:a-widget.expected;return Scaffold(appBar:AppBar(title:const Text('Arqueo')),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Ventas: C\$${widget.sales}'),Text('Préstamos/retiros: - C\$${widget.loans}'),Text('Consumos: - C\$${widget.cons}'),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Total después de préstamos y consumos',style:TextStyle(fontSize:16,fontWeight:FontWeight.bold)),const SizedBox(height:6),Text('C\$${widget.expected}',style:const TextStyle(fontSize:26,fontWeight:FontWeight.bold))])),),const SizedBox(height:12),TextField(controller:c,onChanged:(_)=>setState((){}),keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Efectivo contado',border:OutlineInputBorder())),if(d!=null)Text(d==0?'Exacto':d<0?'Faltante: C\$${d.abs()}':'Sobrante: C\$${d.abs()}',style:const TextStyle(fontSize:20,fontWeight:FontWeight.bold)),FilledButton(onPressed:a==null?null:()=>Navigator.pop(x,a),child:const Text('Confirmar arqueo'))]));}}
class RefillPage extends StatelessWidget{final List<Product> p;const RefillPage(this.p,{super.key});@override Widget build(BuildContext x){final c={for(final z in p)z.name:TextEditingController(text:'0')};return Scaffold(appBar:AppBar(title:const Text('Rellenar Freezer')),body:ListView(padding:const EdgeInsets.all(16),children:[const Text('Última acción de la jornada',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),...p.map((z)=>Card(child:Padding(padding:const EdgeInsets.all(12),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(z.name,style:const TextStyle(fontWeight:FontWeight.bold)),Text('Bodega: ${z.warehouse} • Freezer: ${z.freezer}'),TextField(controller:c[z.name],keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'Unidades a sacar de bodega'))])))),FilledButton(onPressed:(){final r=<String,int>{};for(final z in p){final n=int.tryParse(c[z.name]!.text)??0;if(n<0||n>z.warehouse)return;r[z.name]=n;}Navigator.pop(x,r);},child:const Text('Confirmar y cerrar jornada'))]));}}
class History extends StatefulWidget{final List<Day> history;const History({super.key,required this.history});@override State<History> createState()=>_HistoryState();}
class _HistoryState extends State<History>{String? month;@override Widget build(BuildContext c){final all=[...widget.history]..sort((a,b)=>b.date.compareTo(a.date));final months=all.map((r)=>DateFormat('yyyy-MM').format(r.date)).toSet().toList();month??=(months.isEmpty?null:months.first);final rs=all.where((r)=>month==null||DateFormat('yyyy-MM').format(r.date)==month).toList();return ListView(padding:const EdgeInsets.all(16),children:[const Text('Historial por meses',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),if(months.isNotEmpty)DropdownButtonFormField<String>(value:month,items:months.map((m)=>DropdownMenuItem(value:m,child:Text(m))).toList(),onChanged:(v)=>setState(()=>month=v),decoration:const InputDecoration(labelText:'Mes')),if(rs.isNotEmpty)FilledButton.icon(onPressed:()=>PdfReport.make(rs,month!),icon:const Icon(Icons.picture_as_pdf),label:const Text('Generar / guardar PDF del mes')),const SizedBox(height:8),...rs.map((r)=>Card(child:ExpansionTile(title:Text(DateFormat('dd/MM/yyyy').format(r.date)),subtitle:Text('Ventas C\$${r.sales} • ${r.difference==0?'Exacto':r.difference<0?'Faltante':'Sobrante'}'),children:[ListTile(title:const Text('Esperado'),trailing:Text('C\$${r.expected}')),ListTile(title:const Text('Contado'),trailing:Text('C\$${r.actual}')),ListTile(title:const Text('Diferencia'),trailing:Text('C\$${r.difference}')),ListTile(title:const Text('Préstamos'),trailing:Text('C\$${r.loans.fold(0,(s,x)=>s+x.amount)}')),ListTile(title:const Text('Consumos'),trailing:Text('C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}'))]))),if(rs.isEmpty)const Text('No hay jornadas cerradas en este mes.')]);}}
class PdfReport{static Future<void> make(List<Day> rs,String month)async{final d=pw.Document();for(final period in [rs.where((r)=>r.date.day<=15).toList(),rs.where((r)=>r.date.day>=16).toList()]){d.addPage(pw.MultiPage(build:(_)=>[pw.Header(level:0,child:pw.Text('Inventario Billar')),pw.Text('Mes $month • ${period.isNotEmpty&&period.first.date.day<=15?'Días 1–15':'Días 16–fin'}'),pw.TableHelper.fromTextArray(headers:['Fecha','Ventas','Préstamos','Consumos','Esperado','Contado','Dif.'],data:period.map((r)=>[DateFormat('dd/MM/yyyy').format(r.date),'C\$${r.sales}','C\$${r.loans.fold(0,(s,x)=>s+x.amount)}','C\$${r.consumptions.fold(0,(s,x)=>s+x.value)}','C\$${r.expected}','C\$${r.actual}','C\$${r.difference}']).toList())]));}await Printing.sharePdf(bytes:await d.save(),filename:'Inventario-Billar-$month.pdf');}}
